// this is so static export-ed next.js project doesn't break
// use this basePath in front of essentially every path in next.js code
// (except for fonts i guess)
export const basePath = process.env.NEXT_PUBLIC_BASE_PATH ?? "";
